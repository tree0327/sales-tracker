# UX 개선 라운드 2 (위험 제거 + 마찰 해소 + 실시간 동기화) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 실사용 관점 UX 분석에서 나온 위험 5건(A)·마찰 6건(B)을 해소하고 Supabase Realtime으로 부부 간 실시간 동기화(C)를 붙인다.

**Architecture:** 파괴적 동작(삭제·로그아웃)은 공용 `ConfirmDialog`를 거치고, 삭제는 실행취소 토스트로 복구 가능하게 한다. 지출/급여 수정은 기존 `updateTransaction`+`InputSheet`에 `editTx` 모드를 얹어 해결한다(매출 행은 이미 있는 `SalesInputModal` 수정 모드로 라우팅). 실시간 동기화는 순수 병합 함수 `applyRealtimeEvent`를 TDD로 만들고 `useLedger`에서 postgres_changes 구독으로 배선한다.

**Tech Stack:** React 19 + Vite + 순수 CSS + Supabase(@supabase/supabase-js v2, Realtime). Vitest 40개 통과 중.

## Global Constraints

- **스택 변경 금지**: Tailwind·Next.js·새 npm 의존성 도입 금지. supabase-js는 이미 설치돼 있고 Realtime을 포함한다.
- **디자인은 Apple(iOS HIG) 계열 유지**: 토큰만 사용 — `--bg --paper --fg --fg-2 --fg-3 --line --fill --accent(#0066CC) --on-accent --ok-* --err-* --warn-* --info-* --r-sm(4) --r-md(12) --r-lg(16) --shadow --shadow-sm`. 새 색 하드코딩 금지. `--accent`를 더 밝은 파랑으로 바꾸지 말 것(연회색 칩 위 AA 때문에 #0066CC로 실측 확정됨).
- **4px 그리드**(뱃지 상하 2px만 예외), **라운드 3단계**, **굵기 400/500/700**, **터치 타깃 ≥ 48px**.
- **날짜는 로컬 기준**: `toISOString().slice(0,10)` 금지. `localYMD`/`buildRecordDateISO`(`src/utils/recordDate.js`) 사용.
- **컴포넌트 테스트 인프라 도입 금지(YAGNI)**: jsdom·testing-library 없음. 로직은 순수 함수로 뽑아 TDD, UI는 최종 프리뷰 검증.
- **한국어 UI·주석·커밋 메시지.**
- **커밋 전 검증**: `npx vitest run` 실패 0(현재 40개, 태스크마다 늘어남) + `npm run build` 성공.
- **`git add -A` 절대 금지**: 루트의 미추적 `DESIGN.md`는 사용자 파일 — 커밋·수정·삭제 금지. 명시적 경로만 add.
- 커밋 메시지 마지막 줄: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### 현재 코드 사실 (구현자가 알아야 할 것)

- `useLedger`(`src/hooks/useLedger.js`)의 `deleteTransaction(id)`은 반환값이 없고 낙관적 삭제+롤백만 한다. `addTransaction(payload)`은 저장된 row를 반환한다. `updateTransaction({id, flow, category, method, amount, memo, date})` 존재.
- `Ledger.jsx`의 토스트는 `notify(msg)` — 문자열만 받고 1.7초 후 사라진다. `.toast` CSS 존재.
- `TxRow`(`src/components/TxRow.jsx`)의 × 버튼은 `onDelete(tx.id)`를 **확인 없이 즉시** 호출한다.
- `HomeScreen.jsx:55` 유저칩이 `onClick={onLogout}` — 즉시 로그아웃.
- `index.html:5` viewport에 `maximum-scale=1.0, user-scalable=no`가 있다(줌 차단).
- `.tabbar`(index.css)는 `height: 64px; padding: 0 8px 4px`로 safe-area 미반영. `.app`은 `padding-bottom: 84px`.
- `InputSheet`의 날짜는 `DATE_OPTS = ['오늘','어제','그저께']` 칩 + `dateFromOpt()`(정오 고정 아님 — 12:00 고정이다. 이 버그도 이번에 고친다: `buildRecordDateISO` 재사용).
- `RecordsScreen`은 `t.flow !== 'transfer'` 필터로 공금충전을 숨긴다 — 공금충전 수정은 이번 범위 밖.
- 모달 공용 CSS: `.modal-scrim .modal-card .modal-title .modal-body .modal-actions .btn-ghost .btn-primary` 존재(`SalesInputModal`/`SalesRecordModal`이 사용).
- `SalesInputModal`은 `<SalesInputModal isOpen type initialData onClose onSave />`, `initialData: { id, original, name, date }`, `onSave(type, amount, name, dateISO)`.

---

## Task 1: 정적 위험 3건 — 줌 허용 · 탭바 safe-area · 홈 ▾ 어포던스

**Files:**
- Modify: `index.html:5`, `src/index.css`(.tabbar/.app), `src/screens/HomeScreen.jsx`(title)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 (정적 수정 묶음)

- [ ] **Step 1: 줌 허용** — `index.html:5`의 viewport에서 `, maximum-scale=1.0, user-scalable=no`를 삭제한다:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
```

- [ ] **Step 2: 탭바 safe-area** — `src/index.css`에서:
`.tabbar`의 `height: 64px;` → `height: calc(64px + env(safe-area-inset-bottom));`, `padding: 0 8px 4px;` → `padding: 0 8px calc(4px + env(safe-area-inset-bottom));`
`.app`의 `padding-bottom: 84px;` → `padding-bottom: calc(84px + env(safe-area-inset-bottom));`

- [ ] **Step 3: 홈 ▾ 어포던스** — `src/screens/HomeScreen.jsx:52`의 정적 span을 버튼으로. 지난달이 궁금하면 기록 탭으로 보낸다:
```jsx
<button className="title" onClick={() => onNav('records')} aria-label="지난달 보기 — 기록으로 이동">{monthLabel}<span className="chev">▾</span></button>
```
(`.app-head .title`은 이미 button 스타일을 겸한다 — `background: none; border: none;`)

- [ ] **Step 4: 검증** — `npx vitest run`(40 pass) && `npm run build` 성공.

- [ ] **Step 5: 커밋**
```bash
git add index.html src/index.css src/screens/HomeScreen.jsx
git commit -m "fix(a11y): 줌 허용 + 탭바 safe-area + 홈 월 라벨을 기록 진입 버튼으로"
```

---

## Task 2: 공용 ConfirmDialog + 로그아웃 확인

**Files:**
- Create: `src/components/ConfirmDialog.jsx`
- Modify: `src/Ledger.jsx`, `src/screens/HomeScreen.jsx`, `src/screens/SettingsScreen.jsx`

**Interfaces:**
- Consumes: 기존 모달 CSS(`.modal-scrim .modal-card .modal-title .modal-body .modal-actions .btn-ghost .btn-primary`)
- Produces: `<ConfirmDialog open title body confirmLabel danger onConfirm onCancel />` — Task 3도 이걸 쓴다.
  - `open: boolean`, `danger: boolean`(확인 버튼을 빨강 배경으로), `onConfirm()`, `onCancel()`

- [ ] **Step 1: ConfirmDialog 작성** — `src/components/ConfirmDialog.jsx`:
```jsx
import { createPortal } from 'react-dom';

// 파괴적 동작(삭제·로그아웃) 공용 확인 모달. 오버레이 클릭 = 취소.
export default function ConfirmDialog({ open, title, body, confirmLabel = '확인', danger = false, onConfirm, onCancel }) {
  if (!open) return null;
  return createPortal(
    <div className="modal-scrim" onClick={onCancel}>
      <div className="modal-card" role="dialog" aria-modal="true" aria-labelledby="confirm-title"
        onClick={(e) => e.stopPropagation()}>
        <h2 id="confirm-title" className="modal-title">{title}</h2>
        {body && <p className="modal-body">{body}</p>}
        <div className="modal-actions">
          <button className="btn-ghost" onClick={onCancel}>취소</button>
          <button className={`btn-primary${danger ? ' btn-danger' : ''}`} onClick={onConfirm}>{confirmLabel}</button>
        </div>
      </div>
    </div>,
    document.body
  );
}
```

- [ ] **Step 2: `.btn-danger` 스타일** — `src/index.css` 끝에:
```css
.btn-primary.btn-danger { background: var(--err-fg); }
```

- [ ] **Step 3: 로그아웃 확인 배선** — `src/Ledger.jsx`:
- `import ConfirmDialog from './components/ConfirmDialog';`
- state 추가: `const [confirmLogout, setConfirmLogout] = useState(false);`
- `const logout = () => supabase.auth.signOut();` 은 유지하되, 화면들에는 `onLogout={() => setConfirmLogout(true)}` 를 넘긴다.
- JSX 마지막(토스트 위)에:
```jsx
<ConfirmDialog open={confirmLogout} title="로그아웃할까요?" confirmLabel="로그아웃"
  onConfirm={() => { setConfirmLogout(false); logout(); }} onCancel={() => setConfirmLogout(false)} />
```
`HomeScreen`·`SettingsScreen`은 props 이름이 같으므로 수정 불필요 — 단 확인 후 실제 동작하는지 Step 4에서 본다.

- [ ] **Step 4: 검증** — vitest 40 pass + build 성공.

- [ ] **Step 5: 커밋**
```bash
git add src/components/ConfirmDialog.jsx src/index.css src/Ledger.jsx
git commit -m "feat(ux): 공용 ConfirmDialog + 로그아웃 확인"
```

---

## Task 3: 기록 삭제 확인 + 실행취소 토스트

**Files:**
- Modify: `src/hooks/useLedger.js`(deleteTransaction 반환값), `src/Ledger.jsx`(확인·undo 토스트), `src/index.css`(토스트 버튼)

**Interfaces:**
- Consumes: Task 2의 `ConfirmDialog`, `useLedger.addTransaction`(재삽입용)
- Produces: `Ledger` 내부 `askDeleteTx(id)` — RecordsScreen/SalesScreen에 `onDelete`로 내려가는 함수가 이것으로 바뀐다. `deleteTransaction(id)`은 삭제된 row(스냅샷)를 반환하게 된다(실패 시 null).

- [ ] **Step 1: `deleteTransaction`이 삭제한 row를 반환하도록** — `src/hooks/useLedger.js`:
```js
  const deleteTransaction = useCallback(async (id) => {
    const snap = transactions;
    const removed = snap.find((t) => t.id === id) || null;
    setTransactions((prev) => prev.filter((t) => t.id !== id));
    const { error: e } = await supabase.from(TX_TABLE).delete().eq('id', id);
    if (e) { setError(e.message); setTransactions(snap); return null; }
    return removed; // 실행취소용 스냅샷
  }, [transactions]);
```

- [ ] **Step 2: Ledger에 삭제 확인 + undo 배선** — `src/Ledger.jsx`:
- state: `const [confirmDelete, setConfirmDelete] = useState(null); // 거래 id | null`
- `const [undoTx, setUndoTx] = useState(null);` — 토스트에 실행취소 버튼을 띄울 스냅샷
- notify 타이머와 별개로 undo는 6초 유지:
```jsx
  const askDeleteTx = (id) => setConfirmDelete(id);
  const doDeleteTx = async () => {
    const id = confirmDelete;
    setConfirmDelete(null);
    const removed = await ledger.deleteTransaction(id);
    if (removed) {
      setUndoTx(removed);
      clearTimeout(toastTimer.current);
      setToast('삭제했어요');
      toastTimer.current = setTimeout(() => { setToast(''); setUndoTx(null); }, 6000);
    }
  };
  const undoDelete = async () => {
    const t = undoTx;
    setUndoTx(null); setToast('');
    if (t) await ledger.addTransaction({ flow: t.flow, amount: t.amount, category: t.category, owner: t.owner, method: t.method, memo: t.memo, date: t.date });
  };
```
- `RecordsScreen`·`SalesScreen`에 내려주던 `onDelete={ledger.deleteTransaction}` 를 **둘 다** `onDelete={askDeleteTx}` 로 교체.
- 토스트 JSX 교체:
```jsx
      <div className={`toast${toast ? ' show' : ''}`}>
        {toast}
        {undoTx && <button className="toast-undo" onClick={undoDelete}>실행취소</button>}
      </div>
```
- ConfirmDialog 추가(로그아웃 것 옆에):
```jsx
      <ConfirmDialog open={confirmDelete !== null} title="기록을 삭제할까요?" body="삭제 직후 실행취소할 수 있어요."
        confirmLabel="삭제" danger onConfirm={doDeleteTx} onCancel={() => setConfirmDelete(null)} />
```
- 주의: `SalesRecordModal` 내부의 자체 확인 모달은 그대로 두면 **확인이 두 번** 뜬다. `SalesRecordModal`에서 자체 확인(`confirmId` state·중첩 scrim)을 제거하고 `onDelete(id)`를 바로 호출하게 단순화하라(부모가 확인을 맡는다).

- [ ] **Step 3: 토스트 버튼 스타일** — `src/index.css`:
```css
.toast-undo { margin-left: 12px; background: none; border: none; color: #7CC0FF; font-weight: 700; font-size: 13px; cursor: pointer; }
```
(토스트 배경이 어두운 계열인지 `.toast` 규칙을 확인하고, 어두우면 위 밝은 파랑 유지 — 대비 4.5:1 이상인지 계산해 보고서에 남겨라. 밝은 배경이면 `var(--accent)`로.)

- [ ] **Step 4: 검증** — vitest 40 pass + build 성공.

- [ ] **Step 5: 커밋**
```bash
git add src/hooks/useLedger.js src/Ledger.jsx src/components/SalesRecordModal.jsx src/index.css
git commit -m "feat(ux): 거래 삭제 확인 + 실행취소 토스트"
```

---

## Task 4: 입력 시트 날짜 달력 + 12시 고정 버그 수정 (TDD)

**Files:**
- Modify: `src/utils/recordDate.js`, `src/utils/recordDate.test.js`, `src/components/InputSheet.jsx`

**Interfaces:**
- Consumes: `buildRecordDateISO`, `localYMD`(기존)
- Produces: `resolveSheetDate(opt, now?)` — `'오늘'|'어제'|'그저께'|'YYYY-MM-DD'` → ISO 문자열. Task 5의 수정 모드도 이걸 쓴다.

- [ ] **Step 1: 실패하는 테스트** — `src/utils/recordDate.test.js`에 추가:
```js
import { resolveSheetDate } from './recordDate.js';

describe('resolveSheetDate', () => {
  it("'오늘'은 현재 시각 그대로", () => {
    const now = new Date(2026, 6, 18, 14, 30, 0);
    const dt = new Date(resolveSheetDate('오늘', now));
    expect(dt.getDate()).toBe(18);
    expect(dt.getHours()).toBe(14); // 12시 고정 버그 회귀 방지
  });
  it("'어제'는 하루 전 + 현재 시각", () => {
    const now = new Date(2026, 6, 18, 9, 5, 0);
    const dt = new Date(resolveSheetDate('어제', now));
    expect(dt.getDate()).toBe(17);
    expect(dt.getHours()).toBe(9);
  });
  it('YYYY-MM-DD 는 그 날짜 + 현재 시각', () => {
    const now = new Date(2026, 6, 18, 22, 45, 0);
    const dt = new Date(resolveSheetDate('2026-07-01', now));
    expect(dt.getMonth()).toBe(6);
    expect(dt.getDate()).toBe(1);
    expect(dt.getHours()).toBe(22);
  });
  it('월초로 어제가 지난달이면 달을 넘긴다', () => {
    const now = new Date(2026, 7, 1, 10, 0, 0); // 8/1
    const dt = new Date(resolveSheetDate('그저께', now));
    expect(dt.getMonth()).toBe(6); // 7월
    expect(dt.getDate()).toBe(30);
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/utils/recordDate.test.js` → FAIL(함수 없음).

- [ ] **Step 3: 구현** — `src/utils/recordDate.js` 끝에:
```js
// 입력 시트의 날짜 선택('오늘'/'어제'/'그저께' 칩 또는 달력 YYYY-MM-DD)을 ISO 로.
// 시각은 항상 now 의 시:분:초 — 예전 '정오 12시 고정' 버그를 대체한다.
export function resolveSheetDate(opt, now = new Date()) {
  if (/^\d{4}-\d{2}-\d{2}$/.test(opt)) return buildRecordDateISO(opt, null, now);
  const d = new Date(now);
  if (opt === '어제') d.setDate(d.getDate() - 1);
  if (opt === '그저께') d.setDate(d.getDate() - 2);
  return buildRecordDateISO(localYMD(d), null, now);
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run` → 44 pass.

- [ ] **Step 5: InputSheet 배선** — `src/components/InputSheet.jsx`:
- `import { resolveSheetDate, localYMD } from '../utils/recordDate';` 추가, 기존 `dateFromOpt` 함수 삭제.
- 날짜 칩 렌더(거래 폼·공금충전 폼 둘 다)를 이렇게 교체 — '달력' 칩을 추가하고, 선택하면 date input 노출:
```jsx
      <div className="field-lab">날짜</div>
      <div className="chips">
        {DATE_OPTS.map((d) => (
          <button key={d} className={`chip ${draft.date === d ? 'on' : ''}`} onClick={() => setTx({ ...tx, date: d })}>{d}</button>
        ))}
        <button className={`chip ${isCustomDate ? 'on' : ''}`} onClick={() => setTx({ ...tx, date: localYMD(new Date()) })}>달력</button>
      </div>
      {isCustomDate && (
        <input type="date" className="memo-input" value={draft.date} max={localYMD(new Date())}
          onChange={(e) => setTx({ ...tx, date: e.target.value })} style={{ marginTop: 8 }} />
      )}
```
`const isCustomDate = /^\d{4}-\d{2}-\d{2}$/.test(draft.date);` 를 거래 폼 상단에 추가(공금충전 폼은 `tr.date` 기준으로 동일 패턴, setTr 사용).
- 저장부의 `date: dateFromOpt(draft.date)` → `date: resolveSheetDate(draft.date)` (공금충전도 동일).

- [ ] **Step 6: 검증 + 커밋**
```bash
npx vitest run && npm run build
git add src/utils/recordDate.js src/utils/recordDate.test.js src/components/InputSheet.jsx
git commit -m "feat(input): 날짜 달력 선택 추가 + 12시 고정 버그 수정 (TDD)"
```

---

## Task 5: 지출·급여 수정 — InputSheet editTx 모드 + 기록 행 탭 진입

**Files:**
- Modify: `src/components/InputSheet.jsx`(editTx), `src/components/TxRow.jsx`(행 탭), `src/screens/RecordsScreen.jsx`(onEdit 전달), `src/Ledger.jsx`(배선·매출 분기)

**Interfaces:**
- Consumes: `updateTransaction`(useLedger, 이미 있음), `resolveSheetDate`(Task 4), `SalesInputModal`(수정 모드), `buildRecordDateISO`
- Produces: `<InputSheet ... editTx={tx|null} onUpdateTx={fn} />` — editTx 있으면 수정 모드. `onUpdateTx({id, flow, amount, category, owner, method, memo, date})`.

- [ ] **Step 1: InputSheet에 editTx 모드** — `src/components/InputSheet.jsx`:
- props에 `editTx = null, onUpdateTx` 추가.
- `buildTx` 교체:
```js
function buildTx(preset, role, editTx) {
  if (editTx) {
    return {
      flow: editTx.flow === 'expense' ? '지출' : '수입',
      amount: String(editTx.amount || ''),
      cat: editTx.category, who: editTx.owner, method: editTx.method,
      memo: editTx.memo || '', date: localYMD(editTx.date), whoAuto: false,
    };
  }
  const base = { flow: '지출', amount: '', cat: null, who: role, method: '카드', memo: '', date: '오늘', whoAuto: true };
  if (preset === '급여') Object.assign(base, { flow: '수입', cat: '급여', who: 'husband', method: '계좌', whoAuto: false });
  return base;
}
```
호출부: `useState(() => (mode === 'tx' ? buildTx(preset, member.role, editTx) : null))`.
- 수정 모드에서는 날짜가 처음부터 `YYYY-MM-DD`(달력 칩 활성)로 온다 — Task 4의 `isCustomDate` 로직이 그대로 처리한다.
- `save`:
```js
  const save = () => {
    if (!amt) return notify('금액을 입력해주세요');
    if (!draft.cat) return notify('카테고리를 선택해주세요');
    const payload = {
      flow: draft.flow === '지출' ? 'expense' : 'income',
      amount: amt, category: draft.cat, owner: draft.who, method: draft.method, memo: draft.memo,
      // 수정: 원래 기록의 시각 보존(날짜만 교체). 신규: 칩/달력 → 현재 시각.
      date: editTx && /^\d{4}-\d{2}-\d{2}$/.test(draft.date)
        ? buildRecordDateISO(draft.date, editTx.date)
        : resolveSheetDate(draft.date),
    };
    if (editTx) onUpdateTx({ id: editTx.id, ...payload });
    else onSaveTx(payload);
  };
```
(import에 `buildRecordDateISO` 추가.)
- 저장 버튼 라벨: 수정 모드면 `'수정 저장'`.
- 수입 카테고리 제약: 수정 모드에서 `editTx.category`가 `INC_CATS`에 없으면(이론상 '매출'은 부모가 걸러주지만 방어) 칩 목록에 그 카테고리를 추가로 렌더해 선택 상태가 보이게 하라.

- [ ] **Step 2: TxRow 행 탭 진입** — `src/components/TxRow.jsx`:
- props에 `onEdit` 추가. 행 본문(× 버튼 제외)을 탭하면 수정:
```jsx
    <div className={`tx${onEdit ? ' tappable' : ''}`} onClick={onEdit ? () => onEdit(tx) : undefined}
      role={onEdit ? 'button' : undefined} tabIndex={onEdit ? 0 : undefined}
      onKeyDown={onEdit ? (e) => { if (e.key === 'Enter') onEdit(tx); } : undefined}>
```
- × 버튼은 이벤트 전파 차단: `onClick={(e) => { e.stopPropagation(); onDelete(tx.id); }}`.
- `src/index.css`: `.tx.tappable { cursor: pointer; } .tx.tappable:hover { background: var(--fill); border-radius: var(--r-md); }`

- [ ] **Step 3: RecordsScreen → Ledger 배선** — `RecordsScreen`·`DayGroup`·`TxRow` 체인에 `onEdit` prop을 통과시키고, `Ledger.jsx`에서:
```js
  const [editTx, setEditTx] = useState(null);
  const [salesEdit, setSalesEdit] = useState(null); // 기록 탭에서 매출 행 수정 시
  const openEditTx = (t) => {
    if (t.flow === 'income' && t.category === '매출') {
      // 매출은 수수료 로직이 있는 전용 모달로
      setSalesEdit({ id: t.id, original: t.amount, name: t.memo || '', date: t.date, type: t.method });
      return;
    }
    setSheetMode('tx'); setSheetPreset(null); setEditTx(t); setOpenKey((k) => k + 1); setSheetOpen(true);
  };
  const updateTx = async (payload) => {
    const row = await ledger.updateTransaction(payload);
    closeSheet(); setEditTx(null);
    if (row) notify('수정했어요');
  };
```
- `openInput`/`openFixed`/`openTransfer`는 `setEditTx(null)`을 함께 하도록 수정(신규 입력에 수정 잔상 방지).
- `<InputSheet ... editTx={editTx} onUpdateTx={updateTx} />`.
- `<RecordsScreen ... onEdit={openEditTx} />`.
- 기록 탭 매출 수정용 `SalesInputModal` 렌더(기존 매출 탭 것과 별개 인스턴스):
```jsx
      {salesEdit && (
        <SalesInputModal key={`rec-edit-${salesEdit.id}`} isOpen type={salesEdit.type}
          initialData={salesEdit} onClose={() => setSalesEdit(null)}
          onSave={(type, amount, name, dateISO) => updateSales({ id: salesEdit.id, method: type, amount, memo: name, date: dateISO })} />
      )}
```
(import 추가: `import SalesInputModal from './components/SalesInputModal';`)

- [ ] **Step 4: 검증 + 커밋**
```bash
npx vitest run && npm run build
git add src/components/InputSheet.jsx src/components/TxRow.jsx src/screens/RecordsScreen.jsx src/Ledger.jsx src/index.css
git commit -m "feat(ux): 기록 행 탭으로 지출·급여 수정 (매출 행은 전용 모달로)"
```

---

## Task 6: 연속 입력 + 공금 잔고 홈 노출

**Files:**
- Modify: `src/Ledger.jsx`(saveTx), `src/screens/HomeScreen.jsx`(공금 카드)

**Interfaces:**
- Consumes: `jointStat`(Ledger에서 이미 계산: `{ deposits, spent, balance, contrib }`)
- Produces: `<HomeScreen ... jointBalance={number} />`

- [ ] **Step 1: 홈 튕김 제거** — `Ledger.jsx`의 `saveTx`에서 `setTab('home');` 줄 삭제. (시트는 닫고, 현재 탭에 머문다 — 연속 입력은 ＋를 다시 누르면 되고 화면 맥락이 유지된다.)

- [ ] **Step 2: 공금 잔고 홈 카드** — `HomeScreen`에 `jointBalance` prop 추가, 메뉴 카드 목록의 지출관리 다음에:
```jsx
        <MenuCard icon="◎" iconCls="fx-ic" name="공금 통장" amount={`${fmt(jointBalance)}원`} color="var(--fg)" onClick={() => onNav('expense', 'joint')} />
```
(`MenuCard` 컴포넌트 시그니처는 파일 상단에 있다 — 그대로 따르라.) `Ledger.jsx`에서 `<HomeScreen ... jointBalance={jointStat.balance} />`.

- [ ] **Step 3: 검증 + 커밋**
```bash
npx vitest run && npm run build
git add src/Ledger.jsx src/screens/HomeScreen.jsx
git commit -m "feat(ux): 저장 후 현재 탭 유지(연속 입력) + 공금 잔고 홈 노출"
```

---

## Task 7: Supabase Realtime 실시간 동기화 (TDD)

**Files:**
- Create: `src/utils/realtimeMerge.js`, `src/utils/realtimeMerge.test.js`, `supabase/migrations/0010_realtime.sql`
- Modify: `src/hooks/useLedger.js`

**Interfaces:**
- Consumes: `supabase`(클라이언트), `TX_TABLE` 등 테이블명 상수
- Produces: `applyRealtimeEvent(list, event, opts?)` — `event: { eventType: 'INSERT'|'UPDATE'|'DELETE', new: row|{}, old: {id}|{} }`, `opts: { sortByDateDesc?: boolean }`. 반환: 새 배열(불변).

- [ ] **Step 1: 실패하는 테스트** — `src/utils/realtimeMerge.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { applyRealtimeEvent } from './realtimeMerge';

const a = { id: 1, amount: 100, date: '2026-07-15T03:00:00Z' };
const b = { id: 2, amount: 200, date: '2026-07-16T03:00:00Z' };

describe('applyRealtimeEvent', () => {
  it('INSERT: 새 행을 넣고 date 내림차순 정렬', () => {
    const c = { id: 3, amount: 300, date: '2026-07-17T03:00:00Z' };
    const out = applyRealtimeEvent([b, a], { eventType: 'INSERT', new: c, old: {} }, { sortByDateDesc: true });
    expect(out.map((r) => r.id)).toEqual([3, 2, 1]);
  });
  it('INSERT: 이미 있는 id 면 중복 삽입하지 않는다(자기 발신 낙관적 갱신과의 중복)', () => {
    const out = applyRealtimeEvent([b, a], { eventType: 'INSERT', new: { ...a }, old: {} }, { sortByDateDesc: true });
    expect(out).toHaveLength(2);
  });
  it('UPDATE: 해당 id 를 교체한다', () => {
    const out = applyRealtimeEvent([b, a], { eventType: 'UPDATE', new: { ...a, amount: 999 }, old: {} });
    expect(out.find((r) => r.id === 1).amount).toBe(999);
  });
  it('UPDATE: 없는 id 면 삽입한다(놓친 INSERT 보정)', () => {
    const c = { id: 3, amount: 300, date: '2026-07-14T03:00:00Z' };
    const out = applyRealtimeEvent([b, a], { eventType: 'UPDATE', new: c, old: {} }, { sortByDateDesc: true });
    expect(out.map((r) => r.id)).toEqual([2, 1, 3]);
  });
  it('DELETE: old.id 로 제거한다', () => {
    const out = applyRealtimeEvent([b, a], { eventType: 'DELETE', new: {}, old: { id: 2 } });
    expect(out.map((r) => r.id)).toEqual([1]);
  });
  it('원본 배열을 변형하지 않는다', () => {
    const src = [b, a];
    applyRealtimeEvent(src, { eventType: 'DELETE', new: {}, old: { id: 1 } });
    expect(src).toHaveLength(2);
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/utils/realtimeMerge.test.js` → FAIL.

- [ ] **Step 3: 구현** — `src/utils/realtimeMerge.js`:
```js
// Supabase Realtime(postgres_changes) 이벤트를 로컬 목록에 병합하는 순수 함수.
// 자기 자신의 낙관적 갱신과 중복 수신돼도 안전해야 한다(id 기준 멱등).
export function applyRealtimeEvent(list, { eventType, new: newRow, old: oldRow }, { sortByDateDesc = false } = {}) {
  let next;
  if (eventType === 'DELETE') {
    next = list.filter((r) => r.id !== oldRow.id);
  } else { // INSERT | UPDATE — 둘 다 id 기준 upsert 로 처리해 순서 뒤바뀜·유실에 강하게
    const exists = list.some((r) => r.id === newRow.id);
    next = exists ? list.map((r) => (r.id === newRow.id ? newRow : r)) : [...list, newRow];
  }
  if (sortByDateDesc) next = [...next].sort((x, y) => new Date(y.date) - new Date(x.date));
  return next;
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run` → 50 pass.

- [ ] **Step 5: useLedger 구독 배선** — `src/hooks/useLedger.js`:
```js
import { applyRealtimeEvent } from '../utils/realtimeMerge';
```
`reload`용 useEffect 아래에 추가:
```js
  // 배우자 기기의 입력·수정·삭제를 실시간 반영. 자기 발신 이벤트는 id 멱등 병합이라 안전.
  useEffect(() => {
    const ch = supabase.channel('ledger-sync')
      .on('postgres_changes', { event: '*', schema: 'public', table: TX_TABLE },
        (e) => setTransactions((prev) => applyRealtimeEvent(prev, e, { sortByDateDesc: true })))
      .on('postgres_changes', { event: '*', schema: 'public', table: FIXED_TABLE },
        (e) => setFixed((prev) => applyRealtimeEvent(prev, e)))
      .on('postgres_changes', { event: '*', schema: 'public', table: CATEGORY_TABLE },
        (e) => setCategories((prev) => applyRealtimeEvent(prev, e)))
      .on('postgres_changes', { event: '*', schema: 'public', table: BUDGET_TABLE },
        (e) => setBudgets((prev) => applyRealtimeEvent(prev, e)))
      .subscribe();
    return () => { supabase.removeChannel(ch); };
  }, []);
```
주의: `budgets`의 pk 는 `scope`일 수 있다 — `supabase/migrations/0007_budgets.sql`을 읽고 pk 컬럼을 확인하라. `id`가 없으면 budgets 채널은 콜백에서 `reload()`를 부르는 방식으로 대체하고 보고서에 명시하라.

- [ ] **Step 6: 마이그레이션** — `supabase/migrations/0010_realtime.sql`:
```sql
-- 실시간 동기화: 가계부 테이블을 realtime publication 에 추가.
-- DELETE 이벤트에 old.id 가 실리도록 replica identity 는 기본(pk)이면 충분하다.
alter publication supabase_realtime add table public.transactions;
alter publication supabase_realtime add table public.fixed_expenses;
alter publication supabase_realtime add table public.expense_categories;
alter publication supabase_realtime add table public.budgets;
```

- [ ] **Step 7: 검증 + 커밋** — vitest 50 pass + build.
```bash
git add src/utils/realtimeMerge.js src/utils/realtimeMerge.test.js src/hooks/useLedger.js supabase/migrations/0010_realtime.sql
git commit -m "feat(sync): Supabase Realtime 실시간 동기화 (배우자 입력 즉시 반영)"
```
**보고서에 반드시**: 사용자가 Supabase SQL Editor에서 `0010_realtime.sql`을 실행해야 실시간이 켜진다는 것, 실행 전에도 앱은 기존처럼 동작(구독만 조용히 무이벤트)한다는 것.

---

## Task 8: 통합 검증 · 배포

**Files:** 발견된 문제 수정

- [ ] **Step 1: 프리뷰 하네스 전 흐름 검증** — DevPreview(목업)를 임시 생성해 확인 후 제거:
1. 기록 행 탭 → 수정 시트(값 프리필) → 저장 → 반영 / 매출 행 탭 → SalesInputModal
2. × → 확인 모달 → 삭제 → "삭제했어요 실행취소" → 실행취소 → 복구
3. 유저칩 → 로그아웃 확인 모달
4. 입력 시트 '달력' 칩 → date input → 과거 날짜 저장
5. 지출 저장 후 현재 탭 유지
6. 홈 공금 통장 카드 → 지출/공금 이동
7. 홈 ▾ → 기록 탭 이동
8. 대비: `.toast-undo`, `.btn-danger` 실측 4.5:1↑
(Realtime 은 목업으로 검증 불가 — `applyRealtimeEvent` 테스트 + 코드 리뷰로 갈음하고 보고서에 명시)

- [ ] **Step 2: 전체 테스트 + 빌드** — `npx vitest run`(50) && `npm run build`.

- [ ] **Step 3: 커밋 · 푸시 · 배포** — 발견 수정 커밋 후:
```bash
git push origin main && npm run deploy
```
CDN 반영(로컬 dist CSS 해시 = 라이브 참조) 확인.

- [ ] **Step 4: 사용자 안내** — `0010_realtime.sql` 실행 필요(실시간 켜기), 미실행 시에도 앱 정상.

---

## Self-Review

**1. 스펙 커버리지**: A-1 삭제 확인(Task 3), A-2 로그아웃 확인(Task 2), A-3 safe-area(Task 1), A-4 줌(Task 1), A-5 ▾(Task 1) / B-수정(Task 5), B-달력(Task 4), B-연속입력(Task 6), B-공금 홈(Task 6), B-실행취소(Task 3), B-실시간의 근본해결(Task 7) / C-Realtime(Task 7). 전부 매핑됨.
**2. 플레이스홀더**: budgets pk 확인(Task 7 Step 5)과 `.toast` 배경 확인(Task 3 Step 3)은 "파일을 읽고 판단" 지시로 남김 — 실제 파일 확인이 필요한 부분.
**3. 타입 일관성**: `ConfirmDialog(open,title,body,confirmLabel,danger,onConfirm,onCancel)` Task 2 정의 = Task 3 사용 일치. `resolveSheetDate(opt, now?)` Task 4 정의 = Task 5 사용 일치. `applyRealtimeEvent(list, event, opts)` Task 7 내부 일관. `onUpdateTx({id,...payload})` = `updateTransaction` 시그니처와 일치(owner 는 updateTransaction 이 무시 — 기존 동작 유지).
**4. 위험**: Task 5가 가장 크다(InputSheet 4개 모드). Task 3의 SalesRecordModal 이중 확인 제거를 잊으면 UX 회귀 — 명시했음.
